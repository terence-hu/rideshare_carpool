<form action="/create_shared_ride_from_carpool_group" method="post">

  <input type="hidden" name="user_id" value="<%= current_user.id %>">

  <div class="form-group">
    <label for="preferred_trip_id">
      Preferred trip
    </label>

    <%= select_tag(:preferred_trip_id, options_from_collection_for_select(PreferredTrip.all, :id, :created_at, @shared_ride.preferred_trip_id), :class => "form-control") %>
  </div>

  <input type="hidden" name="carpool_group_id" value="<%= @carpool_group.id %>">

  <button class="btn btn-block btn-outline-secondary">
    Create shared ride
  </button>
</form>
